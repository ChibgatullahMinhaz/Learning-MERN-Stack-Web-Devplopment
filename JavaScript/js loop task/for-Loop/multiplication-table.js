let num = 9; 
 for (let i = 1; i <=10; i++) {
    console.log(`${num} * ${i} = ${num * i}`);
 }

//  onother way
 for (let index = 0; index <=10; index++) {
    const result = num + " * "+ index +" = "+ num * index;
    console.log(result);
    
 }