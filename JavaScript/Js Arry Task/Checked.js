const frouts = ["Apple", "Banana", "Orange", "Grapes", "Pineapple"];

const naem = [];

const name = "jambura";

if (Array.isArray(name)) {
    console.log("it's an array");
    
} else {
    console.log("it's not an array");
}